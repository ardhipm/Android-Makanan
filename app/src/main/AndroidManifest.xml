<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.t.makanan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity" />
        <activity android:name=".BahanActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HasilActivity" />
        <activity android:name=".Hasil2Activity" />
        <activity android:name=".KarakteristikActivity" />
        <activity android:name=".TambahActivity" />
        <activity android:name=".NotFoundActivity" />
        <activity android:name=".PagerActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UploadActivity" />
        <activity android:name=".UpdateActivity"></activity>
    </application>

</manifest>